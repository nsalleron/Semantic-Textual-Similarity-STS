[pytest]
testpaths = tests
markers =
    slow: tests that are computationally heavy (e.g. training).
    download: tests that require downloading models from the Hugging Face Hub.
filterwarnings =
    ignore::DeprecationWarning
